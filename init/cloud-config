#cloud-config
timezone: Asia/Tokyo
disable_root: True

packages:
    - git

runcmd:
    - sudo apt update
    - sudo apt install docker.io docker-compose cron -y
    - sudo apt upgrade -y
    - sudo apt autoremove -y
    - sudo gpasswd -a ubuntu docker
    - sudo systemctl reboot
